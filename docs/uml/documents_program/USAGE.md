# Hockey Editor Pro - Professional Hockey Video Analysis

Полнофункциональный видеомонтажер для анализа хоккейных матчей, аналог SportCode/LongoMatch.

## 🎯 Основные возможности

### 1. **Горячие клавиши для создания отрезков**
- **A** - Атака (красная дорожка)
- **D** - Защита (синяя дорожка)
- **S** - Смена (зелёная дорожка)
- **Space** - Play/Pause
- **Ctrl+O** - Открыть видео
- **Esc** - Отменить текущую запись

### 2. **Режим создания отрезков**
- **Первое нажатие** - начало отрезка (тонкая жёлто-оранжевая полоска начинает расти)
- **Второе нажатие** (той же клавиши) - конец отрезка (отрезок фиксируется)
- **Видео НЕ паузируется** во время записи
- Автоматический откат на 3 секунды в начало (настраивается)

### 3. **Визуальная обратная связь**
- На таймлайне видны все отрезки белыми полосками
- При записи отрезка: **мигающая надпись "🔴 REC"** на соответствующей дорожке
- В статусной строке: **"🔴 Атака: Recording..."** → **"✓ Атака: 01:23–01:45"**
- Кнопка A/D/S подсвечивается **жёлтой границей** при активной записи

### 4. **Профессиональный таймлайн**
- **Горизонтальная шкала времени** (мм:сс) с шагом 5 сек
- **3 цветные дорожки** по типам событий (красная, синяя, зелёная)
- **Жёлтая вертикальная линия** (playhead) - текущее время воспроизведения
- **Масштабирование**: Ctrl + колесо мыши (1× до 10×)
- **Клик на таймлайн** - перемотка видео в эту позицию
- **Всплывающие подсказки** при наведении (на разработку)

### 5. **Управление отрезками**
- **Двойной клик** на отрезок в списке - редактирование
- **Кнопка Delete** - удалить выбранный отрезок
- **Кнопка Clear** - очистить все отрезки
- **Кнопка Export** - экспортировать видеоотрезки в MP4

### 6. **Управление видео**
- **Play/Pause** - кнопка или Space
- **Скорость**: 0.5x, 0.75x, 1x, 1.5x, 2x
- **Ползунок** - быстрая перемотка
- **Отображение времени**: MM:SS / MM:SS

## 📦 Установка

```bash
pip install -r requirements.txt
```

## 🚀 Запуск

```bash
python main.py
```

Запуск из корневой папки проекта (там находится main.py).

## 📁 Структура проекта

```
hockey_editor/
├── core/
│   ├── video_controller.py          # Главный контроллер видео
│   ├── video_processor.py           # Загрузка и обработка видео (mpv)
│   ├── event_creation_controller.py # Управление горячими клавишами
│   └── exporter.py                  # Экспорт видеоотрезков
├── models/
│   └── marker.py                    # Модель события (маркер)
├── ui/
│   ├── main_window.py               # Главное окно приложения
│   └── timeline.py                  # Таймлайн с дорожками
└── utils/
    └── time_utils.py                # Утилиты для работы со временем
```

## 🎨 Цвета и стили

| Элемент | Цвет | Код |
|---------|------|-----|
| Дорожка "Атака" | Тёмно-красный | #8b0000 |
| Дорожка "Защита" | Тёмно-синий | #000080 |
| Дорожка "Смена" | Тёмно-зелёный | #006400 |
| Отрезок | Белый (полупрозрачный) | rgba(255,255,255,200) |
| Запись (REC) | Жёлто-оранжевый | #ffc800 |
| Playhead | Жёлтый | #ffff00 |

## ⚙️ Конфигурация

Редактируется в `config.json`:

```json
{
  "pre_roll_ms": 3000,      // Откат на 3 сек (настраивается)
  "post_roll_ms": 0          // Добавление сек в конец (0 по умолчанию)
}
```

## 🐞 Известные ограничения

- Редактирование отрезков (двойной клик) - в разработке
- Рисование на видео (стрелки, круги) - в разработке
- Сохранение проектов в JSON - в разработке

## 🔧 Техника

- **UI Framework**: PySide6 (Qt6 для Python)
- **Видеоплеер**: libmpv (python-mpv)
- **Обработка видео**: moviepy + ffmpeg
- **Python**: 3.10–3.13
- **ОС**: Windows 10/11, Linux, macOS

## 📝 Пример использования

1. **Запустить приложение**
   ```bash
   python main.py
   ```

2. **Открыть видео**
   - Кнопка "📁 Open" или Ctrl+O

3. **Смотреть игру и нажимать клавиши**
   - **A** - начало атаки (нажать ещё раз для конца)
   - **D** - начало защиты
   - **S** - начало смены

4. **На таймлайне видны все отрезки**
   - Белые полоски = отрезки
   - Жёлтая линия = текущее время

5. **Экспортировать**
   - Кнопка "Export" → сохранить MP4

## 💡 Советы

- **Тренер смотрит игру в реальном времени**, нажимает клавиши - видео не паузируется
- **Отрезок автоматически откатывается** на 3 сек в начало (для контекста)
- **Мигающее "REC"** на таймлайне показывает, что идёт запись
- **Масштабирование**: удерживайте Ctrl и крутите колесо мыши для зума
- **Клик на таймлайн** = быстрая перемотка без паузы

## 📞 Поддержка

GitHub: https://github.com/mijeha4/hockey_editor

---

**Hockey Editor Pro** - профессиональное решение для анализа хоккейных видео. 🏒
