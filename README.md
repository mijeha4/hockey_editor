# Hockey Editor Pro - Professional Hockey Video Analysis

Профессиональный видеомонтажер для анализа хоккейных матчей. Аналог SportCode, LongoMatch, Hudl.

**Главная фишка**: Тренер смотрит игру в реальном времени, нажимает клавиши A/D/S - всё сразу видно на таймлайне с мгновенной обратной связью. Отрезки создаются автоматически, видео не паузируется.

## 🎯 Ключевые особенности

- ✅ **Горячие клавиши**: A (Атака), D (Защита), S (Смена) - два нажатия = начало и конец отрезка
- ✅ **Профессиональный таймлайн**: 3 цветные дорожки, белые полоски отрезков, жёлтый playhead, масштабирование
- ✅ **Визуальная обратная связь**: мигающее "🔴 REC" при записи, подсветка кнопок, статус-строка
- ✅ **Видео НЕ паузируется** при создании отрезков (в отличие от старого интерфейса)
- ✅ **Автооткат**: отрезок откатывается на 3 сек назад для контекста (настраивается)
- ✅ **Экспорт**: MP4, JSON, CSV (в разработке)
- ✅ **Поддержка видео**: MP4, AVI, MOV, MKV через ffmpeg

## 📋 Требования

- **Python**: 3.10–3.13
- **ОС**: Windows 10/11, Linux, macOS
- **FFmpeg**: для обработки видео
- **MPV/libmpv**: для воспроизведения

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Системные зависимости (Windows)

**FFmpeg** (видеообработка):
- Скачайте: https://www.gyan.dev/ffmpeg/builds/#release-builds → `ffmpeg-release-essentials.zip`
- Разархивируйте в `C:\dev\ffmpeg`
- Добавьте `C:\dev\ffmpeg\bin` в PATH
- Тест: `ffmpeg -version`

**MPV/libmpv** (видеоплеер):
- Скачайте: https://sourceforge.net/projects/mpv-player-windows/files/ → `mpv-x86_64-[latest].7z`
- Разархивируйте в `C:\dev\mpv`
- Добавьте `C:\dev\mpv` в PATH
- Перезапустите терминал
- Тест: `python -c "import mpv; print('OK')"`

### 3. Запуск

```bash
python main.py
```

## 🎮 Использование

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| **A** | Атака (красная) - нажать дважды (начало → конец) |
| **D** | Защита (синяя) - нажать дважды (начало → конец) |
| **S** | Смена (зелёная) - нажать дважды (начало → конец) |
| **Space** | Play/Pause |
| **Ctrl+O** | Открыть видео |
| **Esc** | Отменить текущую запись |

### Workflow

1. Запустить приложение: `python main.py`
2. Открыть видео: кнопка "📁 Open" или Ctrl+O
3. Нажимать клавиши:
   - **A** - начало атаки → видео продолжает воспроизведение → **A** - конец атаки ✓
   - На таймлайне сразу видна жёлто-оранжевая полоска, которая растёт в реальном времени
   - На дорожке "Атака" (красная) мигает "🔴 REC"
4. Повторить для D и S
5. Экспортировать отрезки: кнопка "Export"

### UI Элементы

- **Видео**: вверху, чёрный экран с кнопками Play/Pause, ползунок, скорость
- **Список отрезков**: справа, двойной клик для редактирования
- **Таймлайн**: внизу видео, 3 дорожки (красная, синяя, зелёная), белые полоски = отрезки
- **Кнопки A/D/S**: внизу экрана, большие, с подсветкой при активной записи
- **Статус**: красная надпись справа внизу

## 📁 Структура проекта

```
hockey_editor/
├── main.py                          # Точка входа
├── requirements.txt                 # Зависимости (PySide6, opencv, mpv и т.д.)
├── hockey_editor/
│   ├── core/
│   │   ├── video_controller.py      # Главный контроллер (управление видео, события)
│   │   ├── video_processor.py       # Загрузка видео (mpv), seek, скриншоты
│   │   ├── event_creation_controller.py  # Горячие клавиши, режимы создания отрезков
│   │   └── exporter.py              # Экспорт отрезков в MP4
│   ├── models/
│   │   └── marker.py                # Dataclass Marker (тип события, начало, конец, заметка)
│   ├── ui/
│   │   ├── main_window.py           # Главное окно (видео + таймлайн + кнопки A/D/S)
│   │   └── timeline.py              # Таймлайн с 3 цветными дорожками, playhead
│   └── utils/
│       └── time_utils.py            # format_time() - конвертация фреймов в MM:SS
├── USAGE.md                         # Подробная документация (горячие клавиши, стили, примеры)
└── README.md                        # Этот файл
```

## ⚙️ Архитектура

### VideoController (core/video_controller.py)
- Главный контроллер приложения
- Управляет воспроизведением (play, pause, seek)
- Интегрирует EventCreationController для горячих клавиш
- Эмитит сигналы для UI (timeline_update, markers_changed, recording_status_changed и т.д.)

### EventCreationController (core/event_creation_controller.py)
- Обрабатывает нажатия горячих клавиш (A/D/S)
- State machine: IDLE → RECORDING → RECORDED
- Поддерживает pre-roll/post-roll (откат/добавление секунд)
- Создаёт Marker объекты при завершении

### MainWindow (ui/main_window.py)
- UI главного окна (видео, таймлайн, кнопки A/D/S, список отрезков)
- Обработка горячих клавиш (keyPressEvent)
- Подсветка кнопок при активной записи

### TimelineWidget (ui/timeline.py)
- Отрисовка таймлайна в реальном времени
- 3 дорожки по типам событий (красная, синяя, зелёная)
- Белые полоски = отрезки, жёлтый playhead, жёлто-оранжевая полоска записи
- Масштабирование (Ctrl + колесо мыши)
- Клик → seek

## 🎨 Визуальный дизайн

### Цвета

| Элемент | Цвет | Назначение |
|---------|------|-----------|
| Дорожка "Атака" | #8b0000 (тёмно-красный) | Атака |
| Дорожка "Защита" | #000080 (тёмно-синий) | Защита |
| Дорожка "Смена" | #006400 (тёмно-зелёный) | Смена игрока |
| Отрезок | #ffffff (белый, полупрозрачный) | Завершённый отрезок |
| REC полоска | #ffc800 (жёлто-оранжевый) | Текущая запись |
| Playhead | #ffff00 (жёлтый) | Текущее время воспроизведения |

### UI Компоненты

- **Кнопки A/D/S**: 120x90px, 16pt Arial Bold, с подсветкой жёлтой границей при активной записи
- **Таймлайн**: 3 дорожки по 50px каждая, шапка 30px
- **Ползунок видео**: от 0 до total_frames
- **Статус-строка**: красная надпись, обновляется при изменении состояния

## 🔧 Конфигурация

Редактируется в `config.json`:

```json
{
  "pre_roll_ms": 3000,      // Откат на сколько мс в начало отрезка (3 сек)
  "post_roll_ms": 0         // Добавление мс в конец отрезка (0 по умолчанию)
}
```

## 🐛 Известные ограничения

- ⏳ Редактирование отрезков (двойной клик) - в разработке
- ⏳ Рисование на видео (стрелки, круги) - в разработке  
- ⏳ Сохранение/загрузка проектов (.json) - в разработке
- ⏳ Экспорт в CSV/Excel/PDF - в разработке

## 📊 Технический стек

- **GUI Framework**: PySide6 (Qt6 для Python)
- **Видеоплеер**: libmpv (python-mpv)
- **Обработка видео**: moviepy + ffmpeg
- **Python версия**: 3.10–3.13
- **ОС**: Windows, Linux, macOS

## 🚢 Запуск в продакшене

```bash
# Создать exe с PyInstaller
pip install pyinstaller
pyinstaller --onefile --windowed --add-data "assets:assets" main.py
# Результат: dist/main.exe
```

## 📝 Примеры

### Создание отрезка "Атака" (A)

1. Во время игры нажимаем **A** - начало фиксируется
   - На таймлайне появляется жёлто-оранжевая полоска с началом в текущем моменте
   - Полоска растёт по мере воспроизведения видео
   - На красной дорожке мигает "🔴 REC"
   - В статусе: "🔴 Атака: Recording..."

2. Когда игровой момент закончился - нажимаем **A** ещё раз
   - Отрезок фиксируется белой полоской
   - Видео автоматически откатывается на 3 сек в начало
   - В статусе: "✓ Атака: 01:23–01:45"

### Экспорт

1. Нажимаем кнопку "Export"
2. Выбираем папку и имя файла
3. Приложение создаёт MP4 со всеми отрезками по очереди
